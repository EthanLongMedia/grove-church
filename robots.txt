# The staging URL is kept out of search by an X-Robots-Tag header that
# Vercel sends only on the *.vercel.app host (see vercel.json), not by
# a rule here. Crawling has to be allowed for that header to be seen —
# a Disallow would hide the noindex rather than enforce it.
User-agent: *
Allow: /

Sitemap: https://grovechurchmidland.com/sitemap.xml
